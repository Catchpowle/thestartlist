<div class="jumbotron">
  <h1>Stepping Up</h1>
</div> <!-- jumbotron -->
    <% @posts.each do |post| %>
<a href=<%= post.url %>>
  <div class="postg">
    <div class="vote pull-right"></div>
    <h1><%= post.title %></h1>
    <p><%= post.description %></p>
    <!-- for when you want to delete things #unscaleable ;) -->
    <!--<%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></p></li> -->
  </div> <!-- postg -->
</a>
<% end %>
